name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: git clone https://github.com/kaibob222/CICD.git
      - run: git pull
      - run: sudo docker-compose up
     
